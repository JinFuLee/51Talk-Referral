# ref-ops-engine 项目规范 (.cursorrules)

## 全局铁律 (Mandatory Rules)
- **语言强制**：无论是思考过程、执行计划，还是所有输出的文档内容，一律使用**简体中文**响应。如果是特殊代码或术语，使用“中文（英文）”格式表示（如：异步（Async））。
- **禁止 Mock 数据**：严禁引入 `isMock`, `MOCK_DATA` 或任何虚拟占位数据。如果 API 无数据，必须返回空结构（HTTP 200 + `[]`），前端必须实现 `loading` / `error` / `empty` 的三态 UI 展示。
- **数据真实性**：所有业务逻辑和分析流程都基于真实的 Excel 数据源解析，不能虚构业绩。
- **币种规范**：统一用一种格式表达币值组合 `$1,234 (฿41,956)`。要求美金在前，泰铢在括号内。严禁前端暴露出人民币（CNY）金额。

## 编程与工程规范 (Engineering Specs)
- **后端 (FastAPI/Python 3.9+)**：
  - 代码必须提供 Type Hints (类型约束)。
  - 服从 `black` 和 `isort` 的代码格式与导包。
  - 必须通过 `mypy` 强类型校验与 `flake8` 的静默检查。
  - 遇到复杂逻辑强制采用拆分模式，严禁编写 1000 行以上的 God Class。
- **前端 (Next.js 14 / TypeScript)**：
  - 基于 App Router 进行渲染，使用 React 18。
  - TS 配置使用 `strict` 模式。无显式 `any` 和 `@ts-ignore` 存在。
  - **网络请求严禁本地随意定义 `fetcher`**，必须并只能引入统一配置的 `swrFetcher` 进行状态与结果管理。
  - 图表采用 Recharts + shadcn/ui 组件。

## 业务专有领域术语集
- **CC** = 前端销售
- **SS** = 后端销售 (数据中常常体现为 EA)
- **LP** = 后端服务 (数据中常常体现为 CM)
- *遇到后端反馈 EA，或者 CM，在前后端的展示和交互语境中必须自动映射成为 SS 及 LP。*
- **指标体系**：
  - 双差额：`目标绝对差` (actual - target) 与 `时间进度差` (actual/target - time_progress)
  - 双日均：`达标需日均` 与 `追进度需日均`。
- **进度特征**：周三不计入核心工作日（权重为0.0），周六与周日的产出权重提升至 1.4 并包含国定假日的处理。
