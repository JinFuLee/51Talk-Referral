---
## 独立评分卡（Scorer）

**评分时间**：2026-03-19
**评分对象**：ref-ops-engine 改造方案评审报告（ri-refactor-review.md）
**评分原则**：独立评分，不受 writer 自评影响；评分以报告实际内容为准，不预设立场

---

| 维度 | 分数 | 扣分理由（具体指向报告位置） | 改进建议 |
|------|------|--------------------------|---------|
| 科学理论 | 15/20 | §2.1 扣分项仅点名3个缺失边界场景（竞态/冷启动/一致性），未提供科学依据支撑"应如何选择"（无参考文献/无基准数据）。改进建议中的 SQLite 选型（"已是项目现有依赖"）属于实用主义断言而非来源B/C级依据，未量化 SQLite vs JSONL 的并发写入差异。 | 在 DataManager 缓存一致性章节引用 Python threading.RLock 文档或 SQLite WAL 模式官方文档作为 B 级来源；为 SQLite 选型补充量化对比（SQLite WAL 并发写 vs append-only JSONL 的 TPS 差异，哪怕是量级估算）。 |
| 系统性 | 14/20 | §2.2 指出"旧代码删除清单不完整"，但 §5 执行计划评审推导的 tag 划分图同样未给出删除清单文件路径（与被评方案相同缺陷）。Docker 部署改造（docker-compose.yml / Dockerfile 挂载路径）在 CLAUDE.md 技术债#8 中有记录，报告未与之联动（遗漏系统性关联）。旧 API 依赖扫描作为 P0 建议正确，但未说明具体 Grep 命令和扫描路径。 | §5 推导的 tag 划分图应补充"各 tag 涉及删除的文件路径列表"，与 P0 建议 A 联动；在部署方案缺口中引用 CLAUDE.md 技术债#8 作为已知背景，说明 docker-compose.yml 挂载路径变更的具体影响范围。 |
| 框架性 | 15/20 | §2.3 扣分"API 契约缺失"后，改进建议仅说"补充 3-5 个核心字段即可"，未给出任何一个端点的 Pydantic 模型骨架示例（自身框架指导不足）。§5 已推导 tag 划分图，但未在 §2.3 改进建议中标注"本报告 §5 已补充 tag 依赖图"，造成两个章节信息未闭环。 | 在 §2.3 改进建议中，至少给出 1 个核心端点（如 `/api/d4/students`）的 Pydantic 响应模型骨架（4-5 字段）；在框架性维度末尾交叉引用 §5 推导的 tag 划分图，说明该图已弥补执行并行度不足的问题。 |
| 可量化 | 12/20 | §5 推导出 26-33 人天工时估算（有依据），但此估算出现在"执行计划评审"章节而非 §2.4 可量化章节，导致 §2.4 改进建议表格仍将"补充工时估算"列为 Before=无估算，与报告自身内容自相矛盾（信息孤岛）。测试覆盖率 Before/After 完全空白（§2.4 仅说"目标 ≥80%"，无 Before 值）。12条技术债的消除量化仅在改进建议 ROI 列出现，正文评审中未量化当前技术债消除影响。 | §2.4 应前向引用 §5 的工时估算，或将估算移至 §2.4 改进建议表格，消除信息孤岛；测试覆盖率 Before 值需从 CLAUDE.md（M31 pytest 105 cases）或 CI 数据推导当前覆盖率基线，哪怕是"当前覆盖率未测量，M31 后无增量测试"这样的声明。 |
| 可溯源 | 15/20 | §2.5 扣分"汇率异常（18.5x）未追溯到 CLAUDE.md 汇率规则"有效，但报告给出的缓解建议（代码注释 `# WARNING: 规划文件业绩值与D1差距18.5x，疑似THB值`）未引用 CLAUDE.md 中 `USD:THB=1:34` 的来源位置（CLAUDE.md 第"币种显示规范"节）。`历史运营指标 sheet` 字段名缺失（§2.5 最后一条扣分）实际影响 ScenarioEngine 和 AttributionEngine 两个组件的开发，报告仅指出"开发者无法独立推导"，未量化影响范围（2个 Engine × 15期历史数据的字段对齐工作量）。 | 在§3 汇率风险说明中，显式注明 CLAUDE.md "币种显示规范"节的汇率定义（USD:THB=1:34）作为权威来源；在 §2.5 最后一条扣分中补充"字段名缺失影响 tag-engine 两个组件，估计额外沟通+返工成本 0.5-1 人天"的量化说明。 |
| SEE 合规 | 11/20 | 子项A（4/10）：改进建议 E（`verify_data_sources.py`）只验证行数和列名存在性，不验证聚合逻辑正确性（D3 末次归属聚合、G1 渠道金额推算）——这是最核心的数值回归风险，脚本设计深度不足以覆盖 §2.6 自标注的❌缺口。子项B（4/6）：报告评估"全场景自适应❌缺失"后，未在改进建议中给出冷启动/文件锁定/网络异常三个场景的具体自适应方案（仅在 §2.6 评估部分描述问题）。子项C（3/4）：改进建议 F（CLAUDE.md 防错条目）给出了具体格式（`🔴 D4列名规范化铁律`），但 R2/R3/R4 的防错条目未给出，复利沉淀不完整。 | 将 `verify_data_sources.py` 扩展为包含数值回归测试的验证脚本：至少验证 D3 末次归属聚合结果与已知基准值的 ±5% 误差容忍；为 R2/R3/R4 同样给出 CLAUDE.md 防错条目格式草稿；在"全场景自适应"缺口处给出最小可行的冷启动处理方案（返回 `{"status":"cold_start","available_from":null}` 结构体）。 |

**总分：82/120**

**判定：未达标**（达标线 102/120，缺 20 分）

---

### Scorer vs Writer 自评差异分析

| 维度 | Writer 自评 | Scorer 独立评分 | 差值 | 差异根因 |
|------|------------|----------------|------|---------|
| 科学理论 | 16 | 15 | -1 | 缺失来源级别标注（B/C级），改进建议属方案设计而非评审佐证 |
| 系统性 | 14 | 14 | 0 | 一致 |
| 框架性 | 15 | 15 | 0 | 一致 |
| 可量化 | 13 | 12 | -1 | §5 工时估算与 §2.4 信息孤岛，测试覆盖率无 Before 基线 |
| 可溯源 | 16 | 15 | -1 | 汇率风险来源定位不精确，字段名缺失影响范围未量化 |
| SEE 合规 | 12 | 11 | -1 | verify_data_sources.py 深度不足，R2-R4 防错条目未给出 |
| **合计** | **86** | **82** | **-4** | — |

---

### 改进目标 Top3（供 T2 迭代用）

**Priority 1 — 可量化信息孤岛修复**

将 §5 已推导的 26-33 人天工时估算回填到 §2.4 可量化章节（或在 §2.4 改进建议中交叉引用），消除"Before=无估算"与报告实际内容的自相矛盾；补充测试覆盖率 Before 基线声明（当前状态：M31 建立 105 pytest + 42 vitest，改造后目标 ≥80%/≥70%）。

预计得分提升：可量化 +3（12→15），消除信息孤岛后可溯源 +1（15→16）。

**Priority 2 — SEE 合规：自动化防线从"行数验证"升级为"数值回归验证"**

将 `verify_data_sources.py` 扩展为包含聚合逻辑验证：① D3 末次归属聚合结果与抽样基准的 ±5% 容忍 ② D2 有效性过滤后的行数比例合理性（有效率 >30%，否则告警）③ 为 R2/R3/R4 提供 CLAUDE.md 防错条目格式草稿（参照 R1 的 `🔴` 条目格式）。

预计得分提升：SEE 合规 +3（11→14）。

**Priority 3 — 框架性：给出至少 1 个 API 端点的 Pydantic 响应模型骨架 + 引用 §5 tag 图闭环**

在 §2.3 改进建议中补充 `/api/d4/students` 的 Pydantic 模型骨架（StudentListResponse，4-5字段），并在该节末尾注明"本报告 §5 已给出 tag 依赖链推导，可直接复用为执行蓝图"，消除框架性章节的自指不足问题；同时在科学理论 SQLite 选型中补充 1 条 B 级来源引用。

预计得分提升：框架性 +2（15→17），科学理论 +1（15→16）。

**迭代后预计总分**：82 + 3 + 3 + 3 = **91/120**（仍未达标）

**达标路径说明**：当前差距 20 分，Top3 修复可提升约 9 分至 91 分。距达标线 102 仍差 11 分。系统性维度（迁移路径/测试策略）和 SEE 合规的自动化防线深度是最大结构性缺口，需要在第二轮评审中重点补充：
- 补充渐进迁移路径章节（预估 +3 系统性）
- 补充测试策略章节（+2 系统性）
- 补充冷启动等边界场景的自适应方案（+2 SEE 合规）
- 补充旧代码删除清单文件路径列表（+1 系统性 + +1 可溯源）

完整补充后预计 91 + 9 = **100/120**，仍距达标线 2 分，需要第三轮精修。

---

*评分者：独立 Scorer（Sonnet，medium effort）*
*评分依据：rules/report-scoring.md（5维+SEE第6维 × 20分，总分120）*
*密封评分原则：评分依据维度定义锚定，不依据 ri-refactor-review.md writer 自评的评分理由*
