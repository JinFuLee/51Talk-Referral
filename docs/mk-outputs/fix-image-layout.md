# fix-image-layout — 图片排版优化产出报告

**commit**: 58d07c86
**文件**: `scripts/lark_bot.py`
**验证**: `uv run python scripts/lark_bot.py followup --dry-run` ✓ 51 张图片生成无报错

## 变更摘要

### generate_cc_image() 修复

| 问题 | 修复前 | 修复后 |
|------|--------|--------|
| 标题三行间距 | `y-=0.25 / 0.45 / 0.20`（中文/日期行可能重叠） | `y-=0.28 / 0.50 / 0.28`（充足分离） |
| 汇总条到第一围场段 | `y-=0.85` | `y-=0.90` |
| CC 名超长 | 无处理，溢出 | > 18 字符字号 15→13，> 22 字符截断 + `…` |
| 超 30 行数据 | 行高固定 0.35 | 行高自适应 0.30（> 30 行时） |
| 学员 ID 溢出 | 无限制 | 最多 12 字符 |
| 日期列宽度 | `last_call[:10] if len > 10` | 统一 `last_call[:10]` |
| 分段标题色 | `#4A4540` | `#44403C`（对齐 SEE Design System） |
| header_h | 3.0 | 3.2 |

### generate_overview_image() 修复

| 问题 | 修复前 | 修复后 |
|------|--------|--------|
| 标题三行间距 | `y-=0.25 / 0.45 / 0.20` | `y-=0.28 / 0.50 / 0.28` |
| header_h | 2.6 | 3.3 |

### 全局颜色阈值调整（_rate_bg_color / _rate_text_color）

| 档位 | 旧阈值 | 新阈值 | 原因 |
|------|--------|--------|------|
| 绿（达标） | ≥ 75% | ≥ 60% | 业务口径对齐 |
| 黄（警戒） | 50-75% | 40-60% | 业务口径对齐 |
| 红（危险） | < 50% | < 40% | 业务口径对齐 |

### 新增辅助函数

```python
def _truncate_name(name: str, max_len: int = 20) -> str:
    """截断超长名字，超过 max_len 字符时末尾加省略号"""
```

## 验证结果

- THCC-paptsaraporn（49 学员，名字 16 字符）：正常显示，行高 0.30，无溢出
- thcc-Zen（7 学员，名字 8 字符）：正常显示，间距充足
- 总览图：打卡率 54.8% 显示黄色（正确，40-60% 区间），围场列有颜色，合计行深色
- 所有 51 张图片生成完毕，文件大小 49KB-306KB 范围内正常
