#!/bin/sh
cd frontend && npx lint-staged
cd .. && STAGED_PY=$(git diff --cached --name-only --diff-filter=ACM | grep '\.py$' | grep -v '^\.claude/') && [ -n "$STAGED_PY" ] && uv run ruff check $STAGED_PY || true
